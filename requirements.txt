# ── Core AI & APIs ─────────────────────────────────────
google-generativeai==0.8.3
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# ── HTTP & Requests ─────────────────────────────────────
requests==2.31.0
httpx==0.27.2

# ── Text-to-Speech ──────────────────────────────────────
kokoro>=0.9.4            # Offline, human-quality TTS (primary — no API/network needed!)
soundfile>=0.12.1        # Required by kokoro for audio I/O
numpy>=1.24.0            # Required by kokoro
edge-tts==7.0.2          # Microsoft Edge TTS (2nd choice — may fail on cloud runners)
gTTS==2.5.1              # Last-resort fallback

# ── Subtitle Generation ─────────────────────────────────
openai-whisper>=20231117  # Local Whisper: synced VTT from any audio file

# ── Image / Video ───────────────────────────────────────
Pillow==10.3.0           # Thumbnail generation

# ── Environment / Utils ─────────────────────────────────
python-dotenv==1.0.1

