# ── Core AI & APIs ─────────────────────────────────────
google-generativeai==0.8.3
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# ── HTTP & Requests ─────────────────────────────────────
requests==2.31.0
httpx==0.27.2

# ── Text-to-Speech ──────────────────────────────────────
edge-tts==7.0.2          # Microsoft Edge TTS (free, high quality)
gTTS==2.5.1              # Google TTS fallback

# ── Image / Video ───────────────────────────────────────
Pillow==10.3.0           # Thumbnail generation

# ── Environment / Utils ─────────────────────────────────
python-dotenv==1.0.1

# ── Instagram Uploader (fallback) ────────────────────────
instagrapi==2.0.0        # Private API backup (may be IP-blocked on cloud runners)
